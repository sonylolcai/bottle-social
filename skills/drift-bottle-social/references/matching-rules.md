# Matching Rules

The MVP matches only by:

- language
- active account status
- adult status
- daily delivery availability

Personality scores are a soft ordering signal after safety filters. The system does not claim psychological accuracy.

The remote service may choose deterministic recipients for testability. Do not promise true randomness unless the remote tool returns that behavior.
